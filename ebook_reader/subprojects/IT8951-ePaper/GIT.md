# Waveshare driver

We incorporated mainline driver, to see where we at in mainline tree see `git.log` and compare it to `git log` on master in https://github.com/waveshareteam/IT8951-ePaper.

Whenever we needed to change mainline implementation we used EBK directive to differentiate our changes from the origin.
